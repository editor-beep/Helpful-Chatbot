# Helpful-Chatbot
An AI for haters of AI

## Security note
This project currently makes Anthropic API requests directly from the browser (`anthropic-dangerous-direct-browser-access`).
Do not use a production API key in this setup. For real deployments, move API calls behind a backend proxy.
